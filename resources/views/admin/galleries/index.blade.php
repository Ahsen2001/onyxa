@extends('layouts.admin')
@section('title','Gallery')
@section('page-title','Gallery')
@section('content')
    <div class="mb-6 flex flex-wrap justify-between gap-3"><form method="GET" class="flex gap-2"><select name="category" class="rounded-lg border border-[#DCC9AD] px-4 py-2"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category')==$category->id)>{{ $category->name }}</option>@endforeach</select><button class="rounded-lg bg-[#8B5E3C] px-4 py-2 text-sm font-semibold text-white">Filter</button></form><a href="{{ route('admin.galleries.create') }}" class="rounded-lg bg-[#8B5E3C] px-4 py-2 text-sm font-semibold text-white">Upload Image</a></div>
    <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">@forelse($galleries as $gallery)<article class="overflow-hidden rounded-xl border border-[#E8DCCB] bg-white shadow-sm"><img src="{{ asset('storage/'.$gallery->image) }}" alt="{{ $gallery->title }}" class="aspect-[4/3] w-full object-cover"><div class="p-4"><p class="font-semibold">{{ $gallery->title ?? 'Gallery Image' }}</p><p class="text-sm text-[#6F665A]">{{ $gallery->category?->name ?? 'Uncategorized' }} - {{ ucfirst($gallery->status) }}</p><div class="mt-4 flex gap-2"><a href="{{ route('admin.galleries.edit',$gallery) }}" class="rounded-lg border border-[#DCC9AD] px-3 py-2 text-sm text-[#8B5E3C]">Edit</a><form method="POST" action="{{ route('admin.galleries.destroy',$gallery) }}" onsubmit="return confirm('Delete this image?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-2 text-sm text-red-700">Delete</button></form></div></div></article>@empty<p class="text-[#6F665A]">No gallery images found.</p>@endforelse</div><div class="mt-5">{{ $galleries->links() }}</div>
@endsection
